class User {
  constructor(id, imageUrl, name, email, password) {
    this.id = id;
    this.imageUrl = imageUrl;
    this.name = name;
    this.email = email;
    this.password = password;
  }
}

export default User;
